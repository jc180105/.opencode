{
  "scripts": {
    "live:start": "powershell -ExecutionPolicy Bypass -File ./scripts/impeccable-live-start.ps1",
    "live:stop": "powershell -ExecutionPolicy Bypass -File ./scripts/impeccable-live-stop.ps1",
    "test": "echo 'Testing framework not configured'",
    "lint": "echo 'Lint not configured'",
    "typecheck": "echo 'Typecheck not configured'",
    "build": "echo 'Build not configured'"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.14.33",
    "sqlite3": "^6.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "axe-core": "^4.11.4",
    "jest-axe": "^10.0.0",
    "vitest": "^4.1.5"
  }
}
